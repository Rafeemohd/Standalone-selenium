/*or create a new repository on the command line
echo "# Standalone-selenium" >> README.md
git init
git add --all
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Rafeemohd/Standalone-selenium.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/Rafeemohd/Standalone-selenium.git
git branch -M main
git push -u origin main

git config --global user.email "rafeemd14@gmail.com"
git config --global user.name "Rafeemohd"
*/


